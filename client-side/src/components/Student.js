// temperary 
import React from 'react';

// Someone will call this function with variables

const student = (props) => {
    return(
            <div>
                <p> student: {props.Fname} learned in JST: {props.Course} </p>
            </div>
    )

    

};

export default student;
